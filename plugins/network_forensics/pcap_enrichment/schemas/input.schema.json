{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["run", "run_single", "show", "clear"],
      "default": "run",
      "description": "Mode: 'run' enriches all PCAP IPs, 'run_single' enriches one IP, 'show' displays cached enrichment, 'clear' removes cached enrichment."
    },
    "table": {
      "type": "string",
      "description": "Fully qualified BigQuery table: project.dataset.table"
    },
    "fields": {
      "type": "string",
      "description": "Comma-separated list of columns to SELECT from the table."
    },
    "ip": {
      "type": "string",
      "description": "Single IP to enrich (for run_single mode)."
    }
  },
  "required": ["mode"],
  "if": {
    "properties": { "mode": { "const": "run" } }
  },
  "then": {
    "required": ["mode", "table", "fields"]
  }
}
